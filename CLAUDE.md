# CLAUDE.md — horobeat シミュレーター 作業原則

ホロビートカードゲーム（2026-11-14 発売予定）のデッキレシピシミュレーター。
本作は **未発売** であり、公式情報は小出しの段階。よって本リポジトリでは
「知らないことを知らないまま扱う」ことを最優先の設計原則とする。

## 大原則

### 1. カードデータはカード画像の印字内容のみ

- カードのデータ（名称・コスト・数値・テキスト・属性など）は、**公開されたカード画像に実際に印字されている文字列のみ** を転記する。
- 印字されていない項目は、他カードからの類推・シリーズ他作からの流用・「普通こうだろう」という常識を含め、一切書かない。
- 画像が不鮮明で読めない箇所は「読めない」であって「たぶんこう」ではない。`unknown` とする。
- カードデータの各レコードには、出典（画像URL・公開媒体）と公開日を必ず持たせる。

### 2. 未確定値は `unknown` を明示的に置く

- 値が分からないフィールドは、`unknown` を明示的に置く。
- **それらしい値で埋めない。** 具体的には以下をすべて禁止する:
  - デフォルト値での穴埋め（`0`, `1`, `""`, `[]` を「とりあえず」入れる）
  - 他カードの値のコピー
  - 平均値・中央値・「バランス的に妥当な値」の算出
  - `null` と `unknown` の混同（値が存在しないことが確定している場合のみ `null`。
    存在するが不明なら `unknown`）
- `unknown` を含むデータは、シミュレーション実行時に暗黙に 0 等へフォールバックさせない。
  計算に到達した時点で明示的にエラー／警告を出し、どのカードのどのフィールドが
  未確定で計算不能かを利用者に伝える。
- 「`unknown` があるとコードが動かないから仮の値を入れる」は本末転倒。
  動かないことが正しい状態である。

### 3. 推測は `docs/rules_guess.md` に隔離し、コードに入れない

- ルールの推測・予想・「こうであってほしい」は、すべて `docs/rules_guess.md` にのみ書く。
- 推測をソースコード、カードデータ、テストの期待値、定数定義に持ち込まない。
- コード中のコメントとしても書かない（コメントはやがて実装される）。
- `docs/rules_guess.md` の記述は、公式情報で裏が取れた時点で
  `docs/rules_confirmed.md` へ出典付きで移す。移動であって複製ではない。
- 推測に基づく実装が必要になった場合は、実装せずに「未確定のため未実装」と
  `docs/rules_guess.md` に残し、判断を保留する。

### 4. ルール情報には出典と日付を併記する

- `docs/rules_confirmed.md` の全項目に、出典（媒体名・URL・画像ファイル名など）と
  情報の公開日を併記する。例: `（出典: 2026-08 公式ティザー画像）`
- 出典を書けない情報は確定情報ではない。`docs/rules_guess.md` へ回す。
- 一次情報（公式サイト・公式SNS・公式配信）と二次情報（まとめ・攻略サイト・
  ユーザー投稿）を区別して明記する。二次情報のみを根拠に確定扱いしない。
- 情報が更新・訂正された場合は、旧記述を削除せず、更新日と共に改訂履歴を残す。

## ドキュメント構成

| ファイル | 役割 | コードへの反映 |
|---|---|---|
| `docs/rules_confirmed.md` | 出典付きの確定情報のみ | 反映してよい |
| `docs/rules_guess.md` | 推測・未確定事項の隔離先 | **反映しない** |
| `CLAUDE.md` | 本原則 | — |

## 迷ったときの判断

- 確定情報か推測か迷ったら、推測として扱う。
- 書くか書かないか迷ったら、`unknown` と書く。
- シミュレーターが未完成であることは許容されるが、
  未確定情報を確定情報として提示することは許容されない。
